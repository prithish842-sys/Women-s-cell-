import React, { useState } from 'react';
import { AlertTriangle, CheckCircle, KeyRound, Save, ShieldCheck } from 'lucide-react';
import api from '../../utils/api.js';

export const FacultySettings: React.FC = () => {
  const [oldPassword, setOldPassword] = useState(''); const [newPassword, setNewPassword] = useState(''); const [confirmPassword, setConfirmPassword] = useState(''); const [saving, setSaving] = useState(false); const [message, setMessage] = useState(''); const [error, setError] = useState('');
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setMessage(''); setError(''); if (newPassword !== confirmPassword) { setError('New passwords do not match.'); return; } setSaving(true); try { const response = await api.put('/auth/change-password', { oldPassword, newPassword }); if (!response.data.success) throw new Error(response.data.message || 'Could not update password.'); setMessage('Password updated successfully.'); setOldPassword(''); setNewPassword(''); setConfirmPassword(''); } catch (err: any) { setError(err.response?.data?.message || (err as Error).message || 'Could not update password.'); } finally { setSaving(false); } };
  return <div className="space-y-5 fade-in-up"><section className="flex flex-col gap-3 rounded-[20px] bg-[linear-gradient(110deg,#eef3ff,#f4f1ff)] p-5 sm:p-7"><div><p className="text-[11px] font-black uppercase tracking-[0.18em] text-[#2563eb]">Account controls</p><h1 className="mt-2 text-3xl font-black tracking-[-0.03em] text-[#071426]">Settings</h1><p className="mt-2 text-sm font-semibold text-[#52617f]">Manage the faculty account settings currently supported by Singa Pen.</p></div></section><section className="grid gap-4 lg:grid-cols-[1fr_0.9fr]"><form onSubmit={submit} className="rounded-xl border border-[#e4eaff] bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#eef3ff] text-[#4f46e5]"><KeyRound className="h-5 w-5" /></span><div><h2 className="text-lg font-black text-[#071426]">Password & Security</h2><p className="mt-1 text-xs font-semibold text-[#64748b]">Change your password through the existing authenticated endpoint.</p></div></div><div className="mt-5 space-y-4"><label className="block text-xs font-bold text-[#52617f]">Current password<input required type="password" value={oldPassword} onChange={(event) => setOldPassword(event.target.value)} className="mt-1 w-full rounded-lg border border-[#dfe7fb] px-3 py-2.5 text-sm outline-none focus:border-[#6d5dfc]" /></label><label className="block text-xs font-bold text-[#52617f]">New password<input required minLength={8} type="password" value={newPassword} onChange={(event) => setNewPassword(event.target.value)} className="mt-1 w-full rounded-lg border border-[#dfe7fb] px-3 py-2.5 text-sm outline-none focus:border-[#6d5dfc]" /></label><label className="block text-xs font-bold text-[#52617f]">Confirm new password<input required minLength={8} type="password" value={confirmPassword} onChange={(event) => setConfirmPassword(event.target.value)} className="mt-1 w-full rounded-lg border border-[#dfe7fb] px-3 py-2.5 text-sm outline-none focus:border-[#6d5dfc]" /></label>{message && <p className="flex items-center gap-2 text-sm font-semibold text-[#059669]"><CheckCircle className="h-4 w-4" />{message}</p>}{error && <p className="flex items-center gap-2 text-sm font-semibold text-[#dc2626]"><AlertTriangle className="h-4 w-4" />{error}</p>}<button disabled={saving} className="inline-flex items-center gap-2 rounded-lg bg-[linear-gradient(135deg,#2563eb,#7c3aed)] px-4 py-2.5 text-sm font-black text-white disabled:opacity-60"><Save className="h-4 w-4" />{saving ? 'Updating...' : 'Update password'}</button></div></form><aside className="rounded-xl border border-[#e4eaff] bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#ecfdf5] text-[#059669]"><ShieldCheck className="h-5 w-5" /></span><h2 className="text-lg font-black text-[#071426]">Account permissions</h2></div><div className="mt-5 space-y-3 text-sm font-semibold text-[#52617f]"><p>Faculty access is protected by JWT authentication and role authorization.</p><p>Student private wellbeing, ICC complaint, and administrator-only records are not exposed here.</p><p>Notification preferences, theme controls, account deletion, and profile editing have no current backend contract and are intentionally not rendered as dead controls.</p></div></aside></section></div>;
};
